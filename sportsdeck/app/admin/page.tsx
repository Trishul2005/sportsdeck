import SectionPage from '@/components/section-page';

export default function AdminPage() {
	return (
		<SectionPage
			title="Moderation"
			description="Review reports, flagged content, and community health signals in one focused moderation space."
		/>
	);
}
