import SectionPage from '@/components/section-page';

export default function SettingsPage() {
	return (
		<SectionPage
			title="Settings"
			description="Manage theme, profile preferences, and the account behavior that shapes your SportsDeck experience."
		/>
	);
}
